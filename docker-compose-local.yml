version: '3.8'

services:
  crawl4ai-mcp-local:
    image: crawl4ai-mcp:local
    container_name: crawl4ai-mcp-claude
    stdin_open: true
    volumes:
      - ./crawls:/app/crawls
    environment:
      - CRAWL4AI_MCP_LOG=INFO
    command: python -m crawler_agent.mcp_server